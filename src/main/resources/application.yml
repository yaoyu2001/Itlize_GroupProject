spring:
  application:
    name: resources
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: LINFENG ZHOU
    password: Xs!241926126
    url: jdbc:mysql://localhost:3306/resources_manager_project

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: false
        globally_quoted_identifiers: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/,http://localhost:8763/eureka/
